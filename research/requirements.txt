# Core research stack (mock profiling, ESN training, plotting).
# GPU / OpenVLA: use requirements-gpu.txt instead (or install both).
#
#   pip install -r requirements.txt
#
# Python 3.10+ recommended.

numpy>=1.23,<3
scipy>=1.10
matplotlib>=3.8
pandas>=2.0
tqdm>=4.27

# Jupyter notebooks (step1_openvla_profiling.ipynb)
ipykernel>=6.29
