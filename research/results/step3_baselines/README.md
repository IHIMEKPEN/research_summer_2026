# Step 3 — Offline control baselines

Fill with:

```bash
python3 -m src.step3_control_baselines --all --episode 0
```

Outputs: `baseline_{zoh,linear,pid}_ep*.json`, `baseline_comparison.csv`, `baseline_comparison_ep*.png`

Mirrored into `results/step3_evaluation/` for the paper pipeline.
