# Step 3 evaluation

Fill with:

```bash
# Offline ZOH/linear + live UnifoLM bridges (omit --mock for paper)
python3 -m src.step3_sim_comparison --episode 0 --duration_s 10
```

Outputs: `sim_comparison_summary.json`, mirrored `baseline_comparison.csv`
